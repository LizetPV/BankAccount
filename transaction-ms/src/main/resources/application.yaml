spring:
  application:
    name: transaction-ms
  data:
    mongodb:
      host: localhost
      port: 27017
      database: transaction

server:
  port: 8083

account:
  service:
    url: http://localhost:8082/api/v1/cuentas

springdoc:
  api-docs:
    enabled: true
    path: /api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html

